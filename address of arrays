#include <iostream>
using namespace std;

int main()
{
    int arr[3];
    int* p = arr;

    cout << "Enter 3 numbers: " << endl;

    for(int i = 0; i < 3; i++)
    {
        cin >> arr[i];
    }

    cout << "Addresses of array elements:" << endl;

    for(int i = 0; i < 3; i++)
    {
        cout << p + i << endl;
    }

    return 0;
}
